spring: 
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: journalApp
      auto-index-creation: true

server:
  port: 8081
  servlet:
    context-path: /journal      

  mail:
    host: smtp.gmail.com
    port: 587
    username: chinmaykhiste2006@gmail.com
    password: ${mail_password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: 
      port: 12345
      username: default
      password: yourpassword
      ssl: true

  weather:
    api:
      key: ${weather_api_key} 

  
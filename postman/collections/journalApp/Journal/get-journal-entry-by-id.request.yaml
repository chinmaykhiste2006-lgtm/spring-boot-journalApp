$kind: http-request
name: Get Journal Entry by ID
description: 'Retrieve a specific journal entry by its ID. Only returns the entry if it belongs to the authenticated user.'
method: GET
url: '{{baseUrl}}/journal/id/{{entryId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{jwt}}'

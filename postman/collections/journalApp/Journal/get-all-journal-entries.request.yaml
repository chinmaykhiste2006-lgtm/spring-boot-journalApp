$kind: http-request
name: Get All Journal Entries
description: 'Retrieve all journal entries for the authenticated user. Returns 404 if no entries exist.'
method: GET
url: '{{baseUrl}}/journal'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{jwt}}'

$kind: http-request
name: Delete Journal Entry
description: 'Delete a journal entry by its ID. Returns 204 on success, 404 if not found.'
method: DELETE
url: '{{baseUrl}}/journal/id/{{entryId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{jwt}}'

$kind: http-request
name: Get All Users
description: 'Retrieve a list of all registered users. Requires ADMIN role. Returns 404 if no users exist.'
method: GET
url: '{{baseUrl}}/admin/all-users'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{jwt}}'
